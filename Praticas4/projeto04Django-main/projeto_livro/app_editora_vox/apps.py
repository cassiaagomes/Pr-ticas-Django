from django.apps import AppConfig


class AppEditoravoxConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    # O nome do pacote deve corresponder ao nome do diretório do app
    name = 'app_editora_vox'
